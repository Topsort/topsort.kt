<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- A real window for the tests that need one: the banner visibility gate runs on
             attach/detach and scroll callbacks that a free-floating view never receives. -->
        <activity android:name="com.topsort.analytics.banners.BannerHostActivity" android:exported="false" />
    </application>
</manifest>
